function Header(props) {
  return <header>
      <div className='container'>
          <h2>Application One</h2>
          {/* <p>{props.message}</p> */}
      </div>
  </header>;
}

export default Header;
